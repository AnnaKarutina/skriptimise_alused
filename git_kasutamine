root kasutajana:
installeeri: apt install git

esmakordselt:
a) seadista:
git config --global user.name "Ees Perenimi"
git config --global user.email oma.email@domeen
git config --global code.editor nano

kasutamine: 
a) projekti kataloogi loomine - kataloog, kus hoian oma skriptid ja muudan need seal
mkdir skriptimise_alused

b) sisene antud kataloogi
cd skriptimise_alused

c) tekita lokaalne repo git süsteemi jaoks antud kataloogis
git init

#

faili iga muudatuse korral:
1) git status - kontrolli, et fail on muudetud
2) git add failinimi - lisa muudetud fail jälgimisele
3) git commit -m "kirjeldus" - koosta muudatuse kirjeldus
4) git log - kontrolli, et commit on salvestatud
